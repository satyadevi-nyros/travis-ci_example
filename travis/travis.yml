# language: python
# python:
#   - "2.7"
# install:
#   - pip install -r requirements.txt

# script:
#   - python manage.py test
language: python
python:
  - "2.6"
  - "2.7"
install:
  - pip install -q -e . --use-mirrors
script:
  - python setup.py test